# Core agent framework
langchain>=0.3.0
langchain-anthropic>=0.3.0
langchain-core>=0.3.0
langgraph>=0.2.50

# Data validation
pydantic>=2.0

# Optimization (deterministic engine — no LLM)
ortools>=9.10

# HTTP for real API calls
requests>=2.31

# Retry with exponential backoff
tenacity>=8.2

# API server (production)
fastapi>=0.111
uvicorn>=0.29

# Optional production dependencies (uncomment when ready)
# redis>=5.0               # caching + conversation state
# psycopg2-binary>=2.9     # Postgres for user profiles
# pgvector>=0.2            # semantic recipe search (RAG upgrade)
# pint>=0.23               # robust unit conversion
# ingredient-parser-nlp    # NLP ingredient line parsing

# Observability (uncomment when wiring LangSmith)
# langsmith>=0.1

# Testing
pytest>=8.0
